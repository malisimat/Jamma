# B003 independent batch review

## Outcome

**APPROVE**

No blocking finding remains in B003. The implementation is surgical, stays within the approved F-048 boundary, and removes the identified direct out-of-range floating-to-Windows-`unsigned long` conversion by routing the target maximum through the existing checked/saturating conversion.

## Reviewed range and ownership

- Base: `e63f83c` (completed B002 evidence).
- Prerequisite: `d1b6a7698a51078d1363032888d6ce3c67b633ca` (`test(B003 F-048): lock seed conversion bounds prerequisite`).
- Implementation: `749ff1ca98f86a061d2cc1732eca7291548afcb7` (`fix(B003 F-048): saturate seed target conversion before cast`).
- Artifact reconciliation: `be459b26bdb4e5f663180b5d999265ee85e50922` (`docs(B003 F-048): reconcile seed conversion evidence`).
- Production ownership is confined to `JammaLib/src/engine/Quantiser.cpp`; prerequisite coverage is confined to the already registered `test/JammaLib_Tests/src/engine/Quantisation_Tests.cpp`; the remaining changes are the required backlog/finding/verification evidence. No project registration change was necessary because `Quantisation_Tests.cpp` was already compiled by `JammaLib_Tests.vcxproj`.

The range introduces no UserConfig parser/schema edit, broad config hardening, new helper/class/state machine, remote request/authority/lifecycle change, callback behavior, upstream NJClient edit, HUD/VST/window/tooling/MIDI/resource/generated-asset collateral, or F-020/F-045/F-049/F-050 change.

## Conversion and behavior review

- `DeduceSeedTiming` now passes `max(minSeed, sampleRate * targetMaxMs / 1000.0)` to `_RoundedToUInt` before widening its bounded `unsigned int` result to Windows `unsigned long`. The range contains no remaining direct target-maximum floating-to-`unsigned long` cast.
- `_RoundedToUInt` preserves the existing round-to-nearest policy for positive finite values, returns zero for nonpositive values, and saturates values at or above `UINT_MAX`. Its inputs here are products of unsigned policy/sample-rate values converted to `double`, so they are finite and cannot be NaN.
- The exact-fit (`UINT_MAX * 1000 / 1000`), first-overflow (`UINT_MAX * 1001 / 1000`), and `UINT_MAX` policy assertions establish the shared helper boundary; default, zero, supported-high-rate, and minimum-greater-than-target-maximum cases then exercise `DeduceSeedTiming` and assert present, positive, finite, internally coherent geometry.
- P13 does not dynamically drive an extreme overflowing `SeedGrainTargetMaxMs` product through the changed target site. Its exact-fit/overflow assertions reach `_RoundedToUInt` through `MinSeedSamps`, while its `DeduceSeedTiming` cases reach the changed site at non-overflowing values. This is an accurately recorded limitation, not an evidence overclaim: static inspection proves the changed target expression uses that same tested private helper, and the durable artifacts explicitly describe the target-site extreme as structural rather than parser/manual runtime coverage.
- The implementation preserves the existing deterministic `min > target max` policy by taking the maximum of the minimum, one millisecond, and configured target maximum. It neither creates remote authority nor changes local/remote BPI, anchors, phases, cursors, automation origins, follow policy, invalidation, or rebase semantics.
- Rollback is coherent: reverting P13 and the one target-conversion change restores the prior unit; no independent parser/schema/request migration would be stranded.

## Independent verification

Immediately before each build or test invocation I reread `.vscode/tasks.json` and `doc/build.md`. Both incremental builds used the task-authoritative MSBuild executable only through `.github/skills/builder/invoke-msbuild.ps1`, with Debug x64 `Build` and the absolute `SolutionDir` ending in exactly one backslash.

- Incremental `JammaLib/JammaLib.vcxproj` build: exit 0.
- Incremental `test/JammaLib_Tests/JammaLib_Tests.vcxproj` build including references: exit 0.
- `Quantisation.SeedPolicyBoundsConversionBeforeCast` plus `Quantisation.TimingFromSeedAndMasterDerivesBpmAndBpi`: passed 2, failed 0, skipped 0.
- `Quantisation.*`: passed 33, failed 0, skipped 0.
- Related UserConfig timing filter (`DeducesDefaultLoopTimingFromLongLoop`, `DeducesDefaultLoopTimingBelowThreeSecondsWhenPossible`, `LoopTimingHonoursConfiguredTargetMaxGrain`): passed 3, failed 0, skipped 0.
- `git diff --check e63f83c..be459b2`: clean.

## Residual limitations

- No parser/manual extreme-config scenario or live no-malformed-request observation was executed in this batch. The evidence consistently defers that runtime scenario to Stage 21 and does not claim otherwise.
- P13 supplies structural, shared-helper proof rather than direct dynamic target-site overflow coverage. Given the private helper reuse is explicit at the changed line and the focused runtime suites pass, this does not block B003 approval.
