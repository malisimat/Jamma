# B004 independent batch review

## Outcome

**APPROVE**

No blocking finding remains in B004. The characterization-first sequence is truthful, the implementation stays inside the approved observation/numeric boundary, P5/P6 and the complete focused B004 filter are green at the reviewed committed state, and no B005 lifecycle, B006 desired-state, or B009 common-map work was pulled forward.

## Reviewed range and ownership

- Characterization commit: `523714cf115f7d880f8c842f3c461f4b99a0b100` (`test(B004 F-021 F-023 F-026 F-029 F-030 F-031 F-039 F-040): characterize timing observations`). It precedes the production base because `84d3740` is the follow-up sequencing-guidance commit.
- Production base: `84d3740` (`docs(phase4): align remaining sequencing guidance`).
- Explicit-presence, wide-arithmetic, and coherent-local rollback point: `18663f31fa5b533233447244ff006953edf85d3a`.
- Job-owned remote publication and live-adapter deletion: `88e3ecceef9d02ef858fb46f43b4c9baa6e434f9`.
- Canonical evidence reconciliation and reviewed HEAD: `59f3ae3136d67ceeadf0d070078a2122ba32cecc`.

The production diff is confined to the approved B004 owners: `AudioHost`, `Timer`, Jamma-owned timing observation/value/coordinator files, and the existing `NinjamConnection -> NinjamSession -> NinjamController` adapter chain. Test changes are confined to already registered Timer/NINJAM timing and observation files, so no project/filter edit was required. The remaining changes are the required scope/backlog/finding/verification evidence.

The range contains no `Scene`, `Station`, `LoopTake`, common-map, network-lifecycle, desired-state mailbox, upstream NJClient, MIDI/VST implementation, HUD/UI/resource, persistence, generated-asset, or F-020/F-045/F-049/F-050 change. It introduces no class, hierarchy walker, lock, allocation, wait, I/O, formatting, or unbounded traversal on the changed callback path, and introduces no anonymous namespace.

## Findings and invariant review

No actionable B004 finding remains.

- **Remote observation ownership and callback contract:** `NinjamConnection::_UpdateSnapshot` is the sole changed NJClient timing-getter owner at `JammaLib/src/ninjam/NinjamConnection.cpp:951`-`:986`. It accepts a tuple only when the bracketing `_audioProcSequence` remains the same even value, then publishes fixed atomic fields through `_PublishRemoteTiming` (`:546`-`:561`). The callback performs a bounded two-attempt value read (`:564`-`:589`), and the callback-reachable `ProcessExportBlock` body contains only `_client->AudioProc` as an upstream NJClient call (`:592`-`:759`, call at `:747`). The dormant export-compensation path projects that published value instead of calling `GetPosition` (`:627`-`:643`). `GetLiveTiming` is absent from the repository, and the Controller/Session/Connection live-getter adapters were removed only in `88e3ecc`, after the `18663f3` rollback point existed.
- **Separate device and local anchors:** AudioHost captures `Timer::ObserveTransport` before ordinary block advancement, projects the job-owned remote tuple to the current device block coordinate, attaches the local transport value, and publishes one combined observation through the existing mailbox (`JammaLib/src/audio/AudioHost.cpp:442`-`:461`). The value keeps remote/device observation presence and coordinate separate from local master length, phase, count, Timer absolute, and scene position (`JammaLib/src/ninjam/NinjamTiming.h:11`-`:53`). The mailbox includes every field inside its existing fixed-size latest-value protocol (`JammaLib/src/ninjam/NinjamTimingObservationMailbox.h:15`-`:114`).
- **Coordinator ownership and delayed join:** `NinjamTimingCoordinator::Observe` defers when either explicit anchor is absent (`JammaLib/src/ninjam/NinjamTimingCoordinator.cpp:79`-`:86`) and derives join/discipline input from the published local transport rather than job-time Timer scalar reads (`:109`-`:115`, `:148`-`:153`, `:223`-`:244`). `DelayedInitialJoinUsesObservationLocalPhaseNotLiveTimer` runs the same initial join with zero delay and seven 256-sample Timer blocks and requires the same `-375` correction (`test/JammaLib_Tests/src/ninjam/NinjamTimingCoordinator_Tests.cpp:326`-`:352`). No per-loop cursor, anchor, automation-origin, map, or `NoSync` behavior changed, so the protected `M`/`2M`/`3M`/non-divisor and cursor-preservation invariants remain untouched.
- **Presence and projection:** `ResolveBoundaryTimingReplacement` takes explicit optional presence, so numeric sample zero is data while `nullopt` is absence (`JammaLib/src/ninjam/NinjamTiming.h:143`-`:168`). `ToDeviceTiming` carries the remote anchor presence, and `ProjectTimingToAudioSample` refuses absent/backward anchors while recording the bounded observation age (`:184`-`:253`). P6 independently asserts absent, present-zero, and translated present-nonzero replacement behavior (`test/JammaLib_Tests/src/ninjam/NinjamTiming_Tests.cpp:131`-`:156`). The legacy trailing-argument overload deliberately ignores its numeric arguments and derives presence only from `NinjamRemoteTiming`; it does not restore zero-sentinel behavior (`JammaLib/src/ninjam/NinjamTiming.h:218`-`:231`).
- **64-bit Timer arithmetic:** loop count, absolute result, Tick addition, wrap count, and multiplication are widened before arithmetic (`JammaLib/src/utils/Timer.h:79`-`:102`; `JammaLib/src/utils/Timer.cpp:40`-`:58`). P6 proves `UINT32_MAX + 1` exactly and the near-maximum two-wrap/remainder-14 case (`test/JammaLib_Tests/src/ninjam/NinjamTiming_Tests.cpp:158`-`:176`). Timer absolute, scene, and device-audio coordinates remain distinct fields.
- **Downsample tail:** `ScaleWrappedSampleRate` converts the source wrapped phase and clamps it below the already converted target interval (`JammaLib/src/ninjam/NinjamTiming.h:103`-`:115`, used at `:197`-`:205`). P6 proves source phase 95/96 at 96 -> 48 maps to 47/48 rather than manufacturing phase zero (`test/JammaLib_Tests/src/ninjam/NinjamTiming_Tests.cpp:178`-`:192`).
- **P5 truthfulness:** the corrected concurrency test establishes a start barrier, keeps the writer active until at least 64 successful reads, checks incompatible sequence-derived values across every remote/local field, and requires a final sentinel (`test/JammaLib_Tests/src/ninjam/NinjamTimingObservationMailbox_Tests.cpp:85`-`:193`). Its recorded pre-production pass establishes the publication prerequisite; P6's recorded red results correctly characterize missing behavior and are not described as passing prerequisites.

## Independent verification

Immediately before every build or native-test invocation I reread `.vscode/tasks.json` and `doc/build.md`. The incremental build used the task-authoritative `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` only through `.github\skills\builder\invoke-msbuild.ps1`, targeting the absolute `test\JammaLib_Tests\JammaLib_Tests.vcxproj` with `/m /t:Build /p:Configuration=Debug /p:Platform=x64` and an absolute `SolutionDir` ending in exactly one backslash.

- 2026-09-01T15:51:20.2140212-06:00, clean `59f3ae3`: wrapped incremental Debug x64 `JammaLib_Tests` build including references completed with process exit 0.
- 2026-09-01T15:51:30.7525048-06:00, clean `59f3ae3`: `NinjamTimingObservationMailbox.ConcurrentReadProvesOverlapAndCoherence`, `--gtest_repeat=20 --gtest_break_on_failure --gtest_brief=1`; passed 20, failed 0, skipped 0; exit 0.
- 2026-09-01T15:51:37.2032080-06:00, clean `59f3ae3`: `NinjamTiming.PresenceWidthAndDownsampleTailRemainDistinct`; passed 1, failed 0, skipped 0; exit 0.
- 2026-09-01T15:51:45.0834839-06:00, clean `59f3ae3`: `Timer.*:TimerApplyCommand.*:MusicalTransport.*:Ninjam*.*:MidiTimestampMapper.*:MidiBlockTiming.*:MidiClockAnchor.*:Vst2PluginHostCallback.*:VstAudioBuffers.*:Vst3MidiMapping.*`; passed 134, failed 0, skipped 0 across 21 suites; exit 0.
- `git diff --check 84d3740..59f3ae3`: clean. Static call-site search finds no `GetLiveTiming`; the only NJClient timing getters are the four job-owned calls in `_UpdateSnapshot`, and the only NJClient call in callback-reachable `ProcessExportBlock` is `AudioProc`.

## Residual limitations

- No live NINJAM server was available for join/reconnect/disconnect/export-compensation scenarios, and no race tool, ASan, page heap, or Application Verifier run is claimed. Those remain explicit Stage 21 evidence.
- The earlier full 827-test native-suite attempts did not complete: the verbose run saturated its output path and was terminated, while the brief retry stalled and was interrupted. This review does not turn either run into passing evidence. The full native suite remains a mandatory Stage 21 gate.
- Exhaustive small/non-integer sample-rate ratios, a simulated >24-hour live-equivalent session, and first-block live reconnect remain Stage 21 scenarios. These limits do not invalidate the green focused B004 contract or expand B004 into B005/B006/B009.
