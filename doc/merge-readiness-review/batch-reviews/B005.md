# B005 independent batch review

## Superseding outcome

**APPROVE**

This review supersedes the initial `REJECT` recorded in `9e361482f08f53913e91a60587d15f4626692d4c`. The initial review correctly identified B005-R1 (the observation deadline was unreachable through the physically available cached-snapshot path) and B005-R2 (P7 bypassed the replacement and cleanup operations it purported to protect). The red correction characterization at `ab5ea60a9d17458d7f1abe6cad4b819c2368cf9c` and bounded production correction at `789c388a1288ca637a50e3cba8b9e205736f7699` resolve both blockers. The corrected production boundary, lifecycle seams, idempotence, B004 ownership, protected timing coverage, and batch scope independently pass at clean `5a0691eb373644a25ec14a5eb6bd0a0eda6311e6`. B006 may begin after this review commit is reconciled into the canonical batch status.

## Reviewed history and range

- Pre-B005 base: `c2b42643d7fe128b40dbf9059fc6d6fc28316df3` (`docs(B004): record independent approval`).
- Initial characterization: `86dbc2c1481960be224866b1713b2e8a69d49c19` (P7/P8 and project/filter registration).
- Initial implementation: `0fcf7c4` (physical availability/epoch and idempotent recovery) plus `4e99584cb56a6fb588d2a3530fe83889b01ee0ed` (forced replacement-session unavailable edge).
- Initial independent rejection: `9e361482f08f53913e91a60587d15f4626692d4c`, preserving the B005-R1/R2 evidence.
- Review-gap characterization: `ab5ea60a9d17458d7f1abe6cad4b819c2368cf9c`. Its wrapped build failed as expected before the production correction because the production-used Session, Controller, and time-aware NetworkService seams did not yet exist; this red result is characterization evidence, not a passing prerequisite.
- Production correction: `789c388a1288ca637a50e3cba8b9e205736f7699`.
- Canonical correction evidence and reviewed HEAD: `5a0691eb373644a25ec14a5eb6bd0a0eda6311e6`.
- Exact superseding review range: `git diff c2b42643d7fe128b40dbf9059fc6d6fc28316df3..5a0691eb373644a25ec14a5eb6bd0a0eda6311e6`, plus the clean files at reviewed HEAD. `git diff --check` passes.

## Resolution of the initial blockers

### B005-R1 — resolved: the production cached-snapshot path advances the deadline and requires a fresh B004 anchor to recover

`NinjamNetworkService::ObserveTiming` now invokes both `Observe` and `Tick` on every job visit, including visits carrying an available Session's cached latest-value snapshot (`JammaLib/src/ninjam/NinjamNetworkService.cpp:153`-`:174`). Therefore Scene's snapshot branch no longer suppresses deadline advancement even though its no-snapshot fallback remains (`JammaLib/src/engine/Scene.cpp:1394`-`:1416`).

Freshness is derived from B004's existing coherent `AudioBlockStartSample`, not from a second owner or a job-time Timer read. The coordinator updates `_lastValidObservationAt` only when that anchor changes, retains the expired anchor across `ObservationDeadline`, rejects the same cached anchor while deadline `NoSync` is active, and clears the retained identity for other lifecycle-loss reasons (`JammaLib/src/ninjam/NinjamTimingCoordinator.cpp:118`-`:145`, `:306`-`:322`, `:352`-`:387`). Consequently repeated cached observations emit exactly one deadline invalidation and cannot recover authority; a changed audio-block anchor reconnects the tracker and can emit the normal fresh-observation replacement/invalidation result.

Update precedence is deliberate and complete. A fresh observation refreshes the deadline before the same-visit Tick, so its clock/phase/tempo-request update is returned. A cached observation cannot create a new tracker wrap/phase/tempo-request event, and at expiry the deadline invalidation supersedes stale authority. Tick begins with `_ExpireTempoRequest` and then adds deadline invalidation, so a request-expiry prompt remains in the returned update and in `_pendingTempoChange`; it is not silently discarded (`JammaLib/src/ninjam/NinjamTimingCoordinator.cpp:306`-`:349`). The production-boundary regression proves valid timing, one cached-anchor deadline `NoSync`, repeated cached-anchor idempotence/non-recovery, and recovery only after the anchor advances (`test/JammaLib_Tests/src/ninjam/NinjamSessionTiming_Tests.cpp:81`-`:133`).

### B005-R2 — resolved: corrected P7 traverses production-used replacement and cleanup seams

P7 now drives `Controller::LoadConfig` with a persisted NINJAM configuration, which calls the production `Session::Start` path (`JammaLib/src/ninjam/NinjamController.cpp:7`-`:19`; `JammaLib/src/ninjam/NinjamSession.cpp:376`-`:414`). Start publishes a forced unavailable edge before an immediately available replacement can advance the runtime-only epoch; `ObservePhysicalAvailability` is the same narrow Session operation used by Pump (`JammaLib/src/ninjam/NinjamSession.cpp:449`-`:492`).

The test also seeds and clears a pending Controller snapshot through `ApplySessionPumpResult`, proves one physical-loss `NoSync`, proves the repeated loss is empty, proves retry advances epoch 1 to epoch 2, and removes a stale remote station through the NetworkService operation used by Scene (`JammaLib/src/ninjam/NinjamController.cpp:32`-`:51`; `JammaLib/src/engine/Scene.cpp:1405`-`:1411`, `:2250`-`:2253`; `test/JammaLib_Tests/src/ninjam/NinjamSessionTiming_Tests.cpp:10`-`:79`). The persisted/default Scene startup still prepares coordinator state before `Controller::LoadConfig`, while interactive connect and manual disconnect retain their existing prepare/reset/start/stop order (`JammaLib/src/engine/Scene.cpp:254`-`:309`, `:664`-`:671`).

## Lifecycle, ownership, and invariant audit

- Physical loss, invalid timing, and observation deadline all enter the same edge-triggered `_EnterNoSync`; repeated transitions return an empty update. A fresh physical epoch or valid timing reconnects only through the corresponding lifecycle/observation edge. P7, P8, and the cached-boundary test cover all three reasons and recovery.
- Connection loss clears the cached snapshot and published remote tuple only on the job-owned connected-to-failure edge (`JammaLib/src/ninjam/NinjamConnection.cpp:381`-`:390`). `_UpdateSnapshot` remains the sole NJClient timing-getter owner and sole live tuple producer; callback-reachable code still calls only `AudioProc` (`JammaLib/src/ninjam/NinjamConnection.cpp:754`-`:765`, `:960`-`:995`). B005 adds no second timing owner.
- Manual disconnect clears coordinator state and the pending Controller snapshot, while the subsequent unavailable Session observation is idempotent. Replacement Start retains B001's scoped connection lifetime protocol and introduces no callback wait, lock, allocation, logging, I/O, or destruction.
- The protected phase-map model is unchanged: no cursor moves on `NoSync`; no master-modulo/shared-cursor reduction, entity-anchor recapture, map consolidation, Timer geometry rewrite, desired-state/audio-generation gate, or remote BPI fallback entered this batch.

## Scope and collateral audit

The production range is confined to the approved B005 owners: Session, Connection, Controller, NetworkService, TimingCoordinator, and narrow Scene persisted-start/loss/job forwarding. Remaining changes are P7/P8 and their project/filter registration plus canonical evidence/review files. The runtime epoch appears only in Session/Coordinator/tests and is not persisted. No AudioHost, Station, LoopTake, Loop, common-map, desired-state, raw-buffer, JamFile schema, HUD/resource, MIDI/VST implementation, upstream NJClient, generated asset, password/work-directory policy, or broad Scene cleanup changed. No anonymous namespace was added. The correction range `ab5ea60..789c388` is narrower still: existing B005 NINJAM owners and the two existing focused test files only.

## Independent verification

Immediately before every build or native-test invocation, I reread `.vscode/tasks.json` and `doc/build.md`. The build used task-authoritative `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` only through `.github\skills\builder\invoke-msbuild.ps1`, targeting absolute `C:\Users\matto\OneDrive\Source\Jamma\test\JammaLib_Tests\JammaLib_Tests.vcxproj` with `/m /t:Build /p:Configuration=Debug /p:Platform=x64` and `/p:SolutionDir=C:\Users\matto\OneDrive\Source\Jamma\` (exactly one trailing backslash). No Clean, Rebuild, solution build, or full-suite run was used.

- 2026-09-01T22:13:53.3063979-06:00, clean `5a0691e`: wrapped incremental Debug x64 native-test build including references, process exit 0.
- 2026-09-01T22:14:03.4093161-06:00: corrected gate `NinjamSessionTiming.PhysicalLossRetryCreatesOneNoSyncAndFreshEpoch:NinjamTimingCoordinator.NoObservationAndInvalidTimingRecoverIdempotently:NinjamNetworkServiceTiming.CachedObservationDeadlineNoSyncIsIdempotentAndRecoversFresh`; passed 3, failed 0, skipped 0 across 3 suites; exit 0.
- 2026-09-01T22:14:13.7546273-06:00: `NinjamSessionTiming.*:NinjamNetworkServiceTiming.*:NinjamTimingCoordinator.*:NinjamTimingTracker.*:NinjamTimingIntegration.*:NinjamTiming.*:NinjamSessionAudio.StopCannotInvalidateBorrowDuringSynchronousConsume:StationRemote.*`; passed 61, failed 0, skipped 0 across 8 suites; exit 0.
- 2026-09-01T22:14:21.9809446-06:00: `JamFile.DefaultJsonIncludesNinjamConnectionIdentity:JamFile.NinjamConfigParsesOptionalBpmAndBpi:JamFile.NinjamConfigOmitsBpmAndBpiWhenAbsent`; passed 3, failed 0, skipped 0; exit 0.
- 2026-09-01T22:14:28.1413825-06:00: B004 P5 `NinjamTimingObservationMailbox.ConcurrentReadProvesOverlapAndCoherence` with `--gtest_repeat=20 --gtest_brief=1`; passed 20/20 repetitions; exit 0.
- 2026-09-01T22:14:34.1595985-06:00: B004 P6 `NinjamTiming.PresenceWidthAndDownsampleTailRemainDistinct`; passed 1, failed 0, skipped 0; exit 0.
- 2026-09-01T22:14:42.7900232-06:00: protected `Timer.*:TimerApplyCommand.*:MusicalTransport.*:Ninjam*.*:MidiTimestampMapper.*:MidiBlockTiming.*:MidiClockAnchor.*:Vst2PluginHostCallback.*:VstAudioBuffers.*:Vst3MidiMapping.*`; passed 137, failed 0, skipped 0 across 23 suites; exit 0.

## Residual limitations

No live NINJAM server/cable retry, runtime saved/default `.jam` launch through the full Scene UI, manual UI reconnect, sanitizer, page heap, Application Verifier, or full native-suite result is claimed. Those remain assigned to Stage 21. The deterministic tests and static call-chain audit are sufficient for the bounded B005 approval; they do not replace those final runtime checks.
