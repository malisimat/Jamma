# B001 independent batch review

## Decision

**REJECT.** The API-chain refactor correctly extends the apparent guard scope through synchronous `StationRemote` ingestion and stays inside the approved file boundary, but the guard-acquisition protocol itself is not a complete lifetime pin under the C++ memory model. `AudioHost` can therefore still receive a `NinjamConnectionUse` whose connection has already been unpublished without `Stop()` observing that user. P9 acquires its guard before starting `Stop()` and cannot exercise this acquisition/unpublish race. B001 must remain open until that in-boundary lifetime defect is corrected and covered.

## Reviewed baseline, commits, and diff

- Pre-B001 baseline: `1de2ea82a5bd3028c1110cdf1f935299edca1aa9`.
- Prerequisite: `9b46803ea4bf37f0bb7b3488128da02f1ef4546b` (`test(B001 F-033 F-041): add guarded stereo consume prerequisite`).
- Implementation: `996bda2a49de9857f31eb9d9b0f3478f3e28f6da` (`refactor(B001 F-033 F-041): guard remote stereo ingestion lifetime`).
- Artifact reconciliation: `4519f0b23c81bd773ee5e052b5657698bc0a092c` (`docs(B001 F-033 F-041): reconcile lifetime verification evidence`).
- Exact reviewed range: `git diff 1de2ea82a5bd3028c1110cdf1f935299edca1aa9..4519f0b23c81bd773ee5e052b5657698bc0a092c` plus the current files at `HEAD=4519f0b23c81bd773ee5e052b5657698bc0a092c`.
- The range changes 11 files: the five approved production files (`AudioHost.cpp`, `NinjamController.h/.cpp`, `NinjamSession.h/.cpp`), the new P9 test plus its project/filter registration, and the three required reconciliation artifacts. It does not touch NJClient, timing authority/epoch/map code, retained HUD, VST, window/tooling, unrelated MIDI/resources, persistence, or generated assets.
- `git diff --check 1de2ea8..HEAD` passed with no whitespace errors.

## Checks and evidence

### Approved behavior and scope

- `AudioHost.cpp:431` now acquires one `NinjamConnectionUse`; `ConsumeStereoPair` at `:435` returns scratch-buffer pointers while that object remains in scope; `StationRemote::IngestStereoBlock` completes at `:438` before guard destruction. The former `NinjamController::ConsumeStereoPair` and `NinjamSession::ConsumeStereoPair` wrappers, which released the guard before pointer consumption, are removed.
- The production diff adds no dynamic copy, allocation, mutex acquisition, wait, I/O, formatting, final destruction, or new class to the callback call chain. Normal guard release is one atomic decrement; connection disconnect/destruction remains on the lifecycle caller after its wait.
- No protected timing field, coordinate, cursor, anchor, BPI policy, follow policy, recovery behavior, or `NoSync` behavior is changed by B001.
- P9 is present under its approved name and registered in both `JammaLib_Tests.vcxproj` and `.vcxproj.filters`. Its controlled case proves that a guard acquired before `Stop()` keeps `Stop()` from returning until synchronous consumption completes.

### Independent build and focused test rerun

Immediately before each build/test invocation, `.vscode/tasks.json` and `doc/build.md` were reread. The local task's MSBuild executable and Build Tests arguments were used only through `.github/skills/builder/invoke-msbuild.ps1`.

- Commit/time: `4519f0b23c81bd773ee5e052b5657698bc0a092c`, 2026-08-31 23:10 local (`America/Mexico_City`).
- Incremental command: `invoke-msbuild.ps1 -Executable "C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" "C:\Users\matto\OneDrive\Source\Jamma\test\JammaLib_Tests\JammaLib_Tests.vcxproj" /m /t:Build /p:Configuration=Debug /p:Platform=x64 /p:SolutionDir=C:\Users\matto\OneDrive\Source\Jamma\`.
- Incremental result: exit 0; no rebuild diagnostics were emitted.
- Focused command/filter: `JammaLib_Tests.exe --gtest_filter=NinjamSessionAudio.StopCannotInvalidateBorrowDuringSynchronousConsume:StationRemote.IngestStereoBlockFeedsStationMixPath:StationRemote.ZeroThenIngestStereoBlockFeedsStationMixPath`.
- Focused result: exit 0; 3 passed, 0 failed, 0 skipped. Passed by name: `StationRemote.IngestStereoBlockFeedsStationMixPath`, `StationRemote.ZeroThenIngestStereoBlockFeedsStationMixPath`, and `NinjamSessionAudio.StopCannotInvalidateBorrowDuringSynchronousConsume`.
- Repetition command/filter: `JammaLib_Tests.exe --gtest_filter=NinjamSessionAudio.StopCannotInvalidateBorrowDuringSynchronousConsume --gtest_repeat=100 --gtest_brief=1`.
- Repetition result: exit 0; 100/100 repetitions passed, 0 failures, 0 skips.

## Findings

### High — the connection-use acquisition and unpublish protocol has an unclosed entry race

`NinjamSession::_AcquireConnectionUse()` first loads `_connection` at `NinjamSession.cpp:366`, then increments `_activeConnectionUsers` at `:370`, then validates `_connection` with another acquire load at `:371`. Concurrently, `_UnpublishConnectionLocked()` exchanges `_connection` to null at `:352`, observes `_activeConnectionUsers` at `:356`, and transfers `_ownedConnection` at `:359`.

The two atomics use acquire/release operations but have no single total ordering or entry gate. A permitted execution is:

1. The callback's first `_connection` load obtains the published pointer.
2. `Stop()` exchanges `_connection` to null, observes the user count as zero, and transfers ownership without waiting.
3. The callback increments the user count. Its validation load is not required by acquire/release ordering to observe the concurrent null exchange and may still observe the earlier published value, so it returns the pointer as a successful guard.
4. `Stop()` disconnects and can destroy the connection without another user-count wait while `AudioHost.cpp:435` dereferences that pointer or consumes its scratch storage through `:438`.

Thus the new call site has the desired lexical scope but not a guaranteed lifetime pin. This is the same use-after-free class B001/F-033 is required to close, and fixing the existing scoped-use implementation is explicitly inside B001's `NinjamSession.h/.cpp` ownership boundary. The correction must preserve the callback prohibition on locks, waits, allocation, I/O, formatting, and final destruction.

P9 does not detect this race: it completes `NinjamConnectionUse connectionUse(session)` at test line 26 before creating the stop thread at lines 29–33. Its later acquisition loop begins only after the original guard already makes the active count nonzero, so `Stop()` cannot take the zero-user path involved above. Add or correct only the named P9 prerequisite to force acquire/unpublish entry overlap and prove that every successfully returned use was included in retirement; do not add a raw-buffer test API.

### Medium — durable verification metadata is incomplete in the owning matrix

`verification-matrix.md` records aggregate B001 pass claims but not the required command, filter, timestamp, exit code, and pass/fail/skip counts. `cleanup-backlog.md` likewise records only aggregate results. This review supplies independent current-HEAD commands and counts, but it cannot establish the required durable details for the claimed prerequisite-commit run before source movement. Reconcile the owning matrix when B001 is corrected and rerun.

## Rollback integrity

The prerequisite, implementation, and reconciliation changes are separated into the three reviewed commits, and the changed production/test surfaces are isolated from later batches in the reviewed range. Reverting `4519f0b`, `996bda2a`, and `9b46803e` in that order would return to pre-B001 `1de2ea8` without touching the approval-gate record. No destructive rollback was attempted. Because the implementation does not yet establish safe synchronous ownership, this remains the approved fallback if a safe correction cannot be made within B001.

## Limitations

- No live NINJAM server was available, so genuine start/stop/reconnect, clean audio handoff, maximum live block/channel behavior, and physical network timing were not exercised.
- ASan, page heap, Application Verifier, and race tooling were not run; these remain required Stage 21 evidence and are especially relevant to the acquisition race above.
- The prerequisite commit was reviewed as a distinct earlier commit and its current test was rerun, but the reviewer did not switch the shared worktree back to `9b46803e`; the implementer's claim that P9 passed at that exact commit is therefore not independently reproduced.
- The focused `StationRemote` tests exercise synchronous copying independently of a live connection; P9's `127.0.0.1:1` connection never yields a successful scratch-buffer consume. Together they do not exercise the exact successful `AudioHost` acquire/consume/ingest path during concurrent unpublish.
