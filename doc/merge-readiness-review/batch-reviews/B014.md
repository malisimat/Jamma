# B014 independent batch review

## Superseding corrected rereview outcome

**APPROVE**

This fresh rereview supersedes the initial `REJECT` recorded in review commit
`7d55c510d503b3aa287e41aaac580987f82f5b16`. Correction
`1b98fe26cb4b3cc944aab9519624f827ea661696` closes both blockers inside B014's
existing boundary: repeated anomaly and overflow presentation is now
logarithmically rate-bounded, and the post-tracker invalid-local-master-length
guard now emits a dedicated bounded rejection reason. The corrected source,
focused tests, expanded regression filter, and full native suite are green at
canonical tip `85dee4946d89a7bc0cc71e8daf6be18ead073aea`. B014 is independently
approved; B015 may proceed under the sequential gate.

No production, test, or canonical planning file was changed by this rereview.

### Corrected history and ranges

- Original implementation: `058c486ae2588da914703a9efd55a0cd1f811a1e`.
- Initial independent rejection: `7d55c510d503b3aa287e41aaac580987f82f5b16`.
- Narrow correction: `1b98fe26cb4b3cc944aab9519624f827ea661696`.
- Corrected canonical evidence tip: `85dee4946d89a7bc0cc71e8daf6be18ead073aea`.
- Original implementation range: `483c5d4..058c486`; correction range:
  `7d55c510..1b98fe26`; complete B014 evidence range:
  `6312b886^..85dee49`.

`git diff --check` passed with no output for all three ranges. The correction
range changes only `NinjamTimingCoordinator.h/.cpp`, Scene's off-thread
presenter, and focused Coordinator tests. There is no production or test change
after the correction through the canonical tip. The worktree was clean before
this review-file update.

### Blocker 1 closed: bounded and truthful suppression

`NinjamTimingDiagnostics::Capture` increments the selected fixed reason counter
before deciding whether to publish. For every observation/tracker/safety anomaly
it publishes occurrence 1, then only occurrences whose cumulative repeat count
is a power of two: occurrence `2`, `3`, `5`, `9`, and so on. Suppressed calls do
not advance `EventSequence` or `LatestEvent`, so Scene has no new detail record
to print on intervening job ticks. At a published summary,
`OccurrenceCount` equals the complete per-reason counter and
`CumulativeSuppressedCount` equals `OccurrenceCount - 1`; the counter continues
to advance on calls that do not publish. These values therefore describe the
complete observed total rather than only retained or printed records.

After the fixed 32-event array fills, `EventOverflowCount` advances only for a
record that passed the per-reason publication gate. Its presentable
`EventOverflowSummaryCount` changes only when that overflow count reaches a
power of two. Scene keys detail output to `EventSequence` and overflow output to
that summary count, so one indefinitely repeated anomaly cannot create output
on every job tick: detail and overflow lines become exponentially farther apart.
The result remains bounded even when reasons are interleaved because the anomaly
reason set and retained array are fixed.

The two new tests independently exercise both schedules. The repeated-anomaly
test proves all 65 occurrences are counted while only occurrence totals
`1, 2, 3, 5, 9, 17, 33, 65` publish; it also proves the resulting eight
post-capacity publications and the overflow summary threshold. The overflow
test proves the visible threshold sequence `1, 2, 2, 4, 4, 4, 4, 8`.

Desired/no-sync publication and applied-receipt records remain transition
records rather than anomalies and are intentionally outside suppression.
Desired-apply lag remains deduplicated by session epoch plus desired version,
and caught-up remains emitted only when an outstanding lag clears. The existing
two-session correlation test remains green.

### Blocker 2 closed: post-tracker local-length rejection

The actual join/wrap path now captures
`ObservationInvalidLocalMasterLength` at the guard after tracker processing and
before modulo/correction arithmetic when local master length is zero or exceeds
`unsigned int`. Both bounds return without a desired transport, increment the
dedicated reason exactly once, and retain the attempted length (within the
diagnostic field's signed representable range) plus the `unsigned int` limit.
The table-style rejection test drives real wrap events through this guard for
zero and `UINT_MAX + 1`; both cases produce the dedicated latest reason and
counter. The reason is included in the same fixed per-reason storage and anomaly
suppression policy as all other Coordinator rejection reasons.

### Preserved B014 contracts

- Coordinator remains the sole diagnostic-state owner. Storage is a fixed
  `std::array` of 32 events plus fixed counters; capture is `noexcept`, performs
  no allocation, and returns before counter/event work when disabled.
- Scene remains the sole new formatter/presenter. It polls only from
  `OnJobTick` while Event logging is `verbose`; no diagnostic call, branch,
  hierarchy walk, formatter, allocation, wait, mutex, or I/O was added to the
  audio callback or the reviewed Station/LoopTake callback paths.
- The human ownership amendment is exact: the implementation adds only
  `SetTimingDiagnosticsEnabled` and `ObserveAppliedTimingReceipt` as the two new
  mutex-serialized NetworkService forwarders. NetworkService owns no diagnostic
  state, lifecycle/authority behavior, or formatting. The correction does not
  touch either forwarder.
- AudioHost's coherent applied-receipt publication body is unchanged. The
  original B014 implementation only relocates the receipt value to the NINJAM
  timing-value header and updates namespace qualification; the correction does
  not touch AudioHost.
- Static production search finds none of `AlignmentReceipt`,
  `_LogAppliedNinjamLoopAlignment`, `_LogLocalLoopAlignment`,
  `LogLocalLoopAlignment`, or `_ninjamBeforePositions`. The dead receipt,
  hierarchy logger/before-state vector, and record/overdub/delete/correction
  logging call sites remain removed.
- Review of the complete implementation and correction ranges found no Timer,
  map, cursor, request, prompt, follow-policy, epoch, desired-state application,
  or network lifecycle behavior collateral.

### Fresh independent verification

Before every build and native-test invocation, `.vscode/tasks.json` and
`doc/build.md` were reread. The build used task-authoritative
`C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe`
only through `.github\skills\builder\invoke-msbuild.ps1`, targeting the absolute
native-test project with `/m /t:Build /p:Configuration=Debug /p:Platform=x64`
and `/p:SolutionDir=C:\Users\matto\OneDrive\Source\Jamma\` with exactly one
trailing backslash. The unchanged wrapper required the process-local PowerShell
5.1 `ProcessStartInfo.ArgumentList` compatibility shim; no repository file was
modified for it.

- Wrapped incremental Debug x64 native-test build:
  2026-09-05T16:51:04.4322103-06:00 through
  2026-09-05T16:51:37.1348646-06:00; passed.
- `NinjamTimingDiagnostics.*`:
  2026-09-05T16:51:47.0202584-06:00 through
  2026-09-05T16:51:47.1809211-06:00; 7/7 passed, exit 0.
- Exact expanded B014 filter
  `NinjamTimingDiagnostics.*:NinjamTimingCoordinator.*:NinjamSessionTiming.*:NinjamNetworkServiceTiming.*:NinjamTimingProductionBoundary.*-NinjamTimingProductionBoundary.CommonMapCallbackCostIsBoundedAtSaturationCeiling`:
  2026-09-05T16:51:59.5830653-06:00 through
  2026-09-05T16:51:59.9140361-06:00; 42/42 passed across five suites, exit 0.
- Full suite `--gtest_brief=1`:
  2026-09-05T16:52:11.2981517-06:00 through
  2026-09-05T16:52:45.3530808-06:00; 844 tests across 117 suites, 843 passed,
  only the expected hardware-dependent `MidiDevice.OpensPreferredDeviceWhenAvailable`
  skipped, none failed, exit 0. The inherited B009 3,072-entity callback
  benchmark ran and reported `timing_accepted=true`.

Live normal/verbose join, record/overdub, `Stay local`, disconnect/reconnect,
and loss/retry traces were not performed. Release, profiler/race tooling, and
final repository audits likewise remain Stage 21 evidence; they do not leave a
statically or automatically unresolved B014 batch blocker.

## Initial review outcome (retained history)

**REJECT**

B014 is structurally close and all automated verification is green, but two mandatory diagnostics contracts remain open. First, fixed storage does not result in transition-only or rate-bounded output after capacity: every further capture advances `LatestEvent.Sequence` and `EventOverflowCount`, and Scene prints both changes on every verbose job poll. A repeated invalid observation can therefore emit two lines per job tick indefinitely instead of a first occurrence plus a bounded suppression summary. Second, `NinjamTimingCoordinator::Observe` still silently drops a join/wrap when the observed local master length is zero or exceeds `unsigned int`, with no diagnostic reason or counter; the test named `EveryCoordinatorRejectionHasOneBoundedReason` does not exercise that guard. These conflict with accepted F-034/F-047 and B014's bounded-volume and complete-rejection-reason requirements. B015 remains gated.

No production or test file was changed by this review.

## Reviewed history and ranges

- Characterization-only test commit: `6312b8863dd58f65d0fe62e7f339999d7d04cc72`.
- Human-approved ownership amendment: `483c5d4dc60ed9b6e3d96f8b234e8881a51b9aa7`.
- Implementation/final source content: `058c486ae2588da914703a9efd55a0cd1f811a1e`.
- Canonical implementation/evidence tip reviewed: `23c155144dfaedeb23eba4077516b66cb1d18609`.
- Implementation range: `git diff 483c5d4..058c486`; complete B014 range: `git diff 6312b886^..23c1551`.

`git diff --check 483c5d4..058c486` and `git diff --check 6312b886^..23c1551` both passed with no output. The worktree was clean at review start. The implementation range contains only the 14 recorded B014 production/test files: Coordinator diagnostics, the receipt value move, the two new NetworkService forwarders, Scene presentation/removal, Station/LoopTake diagnostic deletion, and focused Coordinator tests. The characterization commit changes tests only; the amendment and canonical reconciliation change review documentation only.

## Blocking findings

### 1. Post-capacity output is not transition-only or rate-bounded

Accepted F-034 requires transition-only/rate-bounded off-thread formatting, and the Stage 17 contract requires repeated anomalies to use first-occurrence reporting plus a suppressed-count summary. The implementation bounds retained array storage to 32 events, but does not bound repeated output over time:

- `NinjamTimingDiagnostics::Capture` updates `LatestEvent` and increments `EventSequence` for every enabled event, including every event after the 32-slot array is full (`JammaLib/src/ninjam/NinjamTimingCoordinator.cpp:24`-`:42`). It also increments `EventOverflowCount` for every post-capacity event.
- `Scene::_LogNinjamTimingDiagnostics` prints `LatestEvent` whenever its sequence advances and separately prints the overflow total whenever that counter advances (`JammaLib/src/engine/Scene.cpp:461`-`:473`).
- `Scene::OnJobTick` polls and presents these values on every verbose job tick (`JammaLib/src/engine/Scene.cpp:1317`-`:1321`). There is no per-reason transition gate, first-occurrence state, elapsed-time limiter, or suppression-summary interval for observation, tracker, or safety anomalies.

Consequently, after the retained array fills, one repeated invalid timing observation per job tick advances both values and remains eligible to print one latest-event line plus one overflow line on every tick indefinitely. This is bounded memory and bounded work per poll, but not the approved bounded-rate/suppressed-volume diagnostic behavior. The five diagnostic tests prove array capacity and overflow accounting only; they do not exercise Scene presentation after overflow or repeated-anomaly suppression. The pending manual bounded trace cannot close a statically absent suppression/rate mechanism.

### 2. A Coordinator rejection path has no reason

After a tracker join/wrap event, `NinjamTimingCoordinator::Observe` rejects phase-command publication when `timing.LocalTransport.MasterLengthSamps` is zero or exceeds `unsigned int` (`JammaLib/src/ninjam/NinjamTimingCoordinator.cpp:376`-`:378`). That return has no `_CaptureDiagnostic` call and no dedicated reason/counter. It occurs after the observation has passed presence/proposal validation and after tracker state/counters may have advanced, so it is a distinct dropped-correction validation path rather than an ordinary duplicate/no-event return.

`NinjamTimingDiagnostics.EveryCoordinatorRejectionHasOneBoundedReason` covers disconnected, zero remote interval, generic invalid, sample-rate, tempo, BPI, missing anchors, invalid derived grid step, tracker-backward, and safety-limit paths, but it does not cover this local master-length guard (`test/JammaLib_Tests/src/ninjam/NinjamTimingCoordinator_Tests.cpp:194`-`:281`). B014 therefore cannot claim complete Coordinator rejection reasons.

## Satisfied structural contracts

The remaining reviewed B014 structure is acceptable and should be preserved when correcting the blockers:

- `NinjamTimingCoordinator` is the sole owner of diagnostic state. The fixed event array has capacity 32, reason counters and overflow accounting are bounded in storage, and no second diagnostic state owner was introduced.
- The ownership amendment is respected. `SetTimingDiagnosticsEnabled` and `ObserveAppliedTimingReceipt` are exactly the two new mutex-serialized NetworkService forwarding operations. The pre-existing `TimingDiagnostics` accessor remains unchanged; NetworkService adds no diagnostic state, formatting, or timing authority/lifecycle behavior.
- Scene is the sole new presenter, and formatting/I/O occurs in `OnJobTick`, not in the audio callback. Event detail is gated by `Event == "verbose"`; the formerly unconditional tempo-connect detail was brought under the same gate.
- The audio callback's coherent receipt publication body is unchanged. B014 only moves the existing receipt value from the `audio` namespace into the NINJAM timing-value header and updates type qualification. No callback diagnostic call, hierarchy traversal, allocation, formatter, I/O, mutex, or wait was added; disabled diagnostics do not add callback work.
- Desired/applied records carry desired epoch/version/generation and applied epoch/version, including stale prior-session receipts. Lag and caught-up transitions are deduplicated by desired epoch/version, and the two-session focused test covers stale epoch correlation.
- The dead LoopTake `AlignmentReceipt` type, reader, and eleven atomics are removed. Scene's former applied hierarchy logger, Station's public/private hierarchy logger, before-position vector/type, and record/overdub/delete/correction call sites are removed. Static search finds none of `AlignmentReceipt`, `_LogAppliedNinjamLoopAlignment`, `_LogLocalLoopAlignment`, `LogLocalLoopAlignment`, or `_ninjamBeforePositions` in production.
- No operational Timer, map, per-entity cursor, request, prompt, epoch, policy, or desired-state application algorithm was intentionally changed. The full regression suite found no behavior collateral.

## Independent verification

Before every build and native-test invocation, `.vscode/tasks.json` and `doc/build.md` were reread. The build used task-authoritative `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` only through `.github\skills\builder\invoke-msbuild.ps1`, targeting the absolute `test\JammaLib_Tests\JammaLib_Tests.vcxproj` with `/m /t:Build /p:Configuration=Debug /p:Platform=x64` and `/p:SolutionDir=C:\Users\matto\OneDrive\Source\Jamma\` (exactly one trailing backslash). The unchanged wrapper required a process-local PowerShell 5.1 `ProcessStartInfo.ArgumentList` compatibility shim; no repository file was modified for that workaround.

- Wrapped incremental Debug x64 native-test build: 2026-09-05T15:24:20.7815432-06:00 through 15:24:22.6181803-06:00; exit 0.
- `--gtest_filter=NinjamTimingDiagnostics.*`: 2026-09-05T15:24:36.2143443-06:00 through 15:24:36.4074811-06:00; 5/5 passed, 0 failed, 0 skipped; exit 0.
- `--gtest_filter=NinjamTimingDiagnostics.*:NinjamTimingCoordinator.*:NinjamSessionTiming.*:NinjamNetworkServiceTiming.*:NinjamTimingProductionBoundary.*-NinjamTimingProductionBoundary.CommonMapCallbackCostIsBoundedAtSaturationCeiling`: 2026-09-05T15:24:49.0233696-06:00 through 15:24:49.4853957-06:00; 40/40 passed across five suites, 0 failed, 0 skipped; exit 0.
- Full suite `--gtest_brief=1`: 2026-09-05T15:25:02.9855654-06:00 through 15:25:55.0049079-06:00; 842 tests from 117 suites, 841 passed, one expected hardware-dependent `MidiDevice.OpensPreferredDeviceWhenAvailable` skip, 0 failed; exit 0. The inherited B009 3,072-entity saturation benchmark ran and reported `timing_accepted=true`.

These green results show no observed runtime regression, but automated success does not override the two statically demonstrated acceptance gaps.

## Required correction and rereview scope

Keep the correction inside B014's existing owners. Add an explicit bounded transition/first-occurrence and suppression-summary policy for repeated anomalies so Scene cannot emit a line every job tick indefinitely after overflow; extend focused tests through the presentation/drain policy where practical. Give the invalid local master-length guard a bounded diagnostic reason/counter and include it in the complete rejection table test. Preserve the fixed-capacity/no-allocation design, sole Coordinator ownership, exactly two newly added NetworkService forwarders, off-callback formatting, and unchanged receipt/callback behavior.

After correction, rerun the wrapped incremental Debug native-test build, `NinjamTimingDiagnostics.*`, the exact 40-test B014 filter, and the full suite, then obtain a fresh independent B014 review. Live normal/verbose join/record/overdub/`Stay local`/disconnect/reconnect bounded-trace evidence, Release, profiler/race tooling, and final repository audits remain Stage 21 unless performed during correction.
