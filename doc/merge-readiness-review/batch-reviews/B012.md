# B012 independent batch review

## Outcome

**APPROVE**

B012 completes the amended F-007, F-008, and F-012 ownership work without a timing-behavior or scope defect. The existing quantisation timing values now live in a registered engine value header; the two amendment-only NINJAM consumers change only their include target; the local transport-offset handoff is private to AudioHost; and the three targeted NINJAM public headers now expose declarations while their non-template runtime bodies live in registered source files. The prerequisite, latest/zero handoff, disconnected `NoSync`, focused timing/mailbox coverage, and full native suite are independently green at clean canonical `a2d2689e4e4dcb399ea7de50fc0971b11ae1e863`. No B012 blocker remains; B013 may begin after this approval is reconciled into canonical status.

## Reviewed history and range

- Pre-implementation amended base: `67778633d60b0292709b4e459402b6bc3099be48` (`docs(B012): approve F-007 include boundary scope`).
- F-007 value-header extraction: `2473d5002bcf9ca350254175dec7a2ff804a987c`.
- F-008 AudioHost-local handoff: `8c15044072b12cba1b6dab8f02331e53533fcf1f`.
- F-012 runtime-body extraction: `c7f668aec2fbadbbd35ed2acbaab43c16e4c4d87`.
- F-007 reverse-include retirement and final source content: `9473d34ff9c87502c720c593e2b6f66c2d843e75`.
- Canonical pending reconciliation and reviewed HEAD: `a2d2689e4e4dcb399ea7de50fc0971b11ae1e863`.
- Reviewed production/test range: `git diff 6777863..9473d34`; full range through canonical evidence: `git diff 6777863..a2d2689`. Both pass `git diff --check`.

The implementation range is confined to the approved B012 owners: Quantiser and the new engine timing-value header; AudioHost; the three targeted NINJAM header/source pairs; necessary project/filter registration; and existing timing-command/integration tests. Under the human amendment, `NinjamTimingCoordinator.h` and `NinjamNetworkService.h` each replace only `#include "../engine/Quantiser.h"` with `#include "../engine/QuantisationTiming.h"`; neither header has a declaration or behavior change. No UI, VST, resource, persistence, generic-mailbox, B013 proposal/BPI, later naming/diagnostic, or unrelated production file entered the batch.

## Ownership and behavior assessment

No blocker was found.

- `engine/QuantisationTiming.h` contains the pre-existing value declarations moved from `Quantiser.h`. Its dependencies are limited to fixed-width/optional value support and the existing constants header, plus the existing forward declaration of the MIDI quantisation enum. It introduces no controller, action, graphics, GUI, audio, VST, or NINJAM dependency and adds no new class.
- No NINJAM production header now includes `Quantiser.h`. `Quantiser.cpp` also no longer includes `NinjamTiming.h`, retiring the reverse edge. Quantiser retains the new value header through its own public header, and all new header/source entries are present in both the project and filter files.
- `LocalTransportOffsetLoopFracMailbox` moved, rather than being generalized or duplicated, into private nested AudioHost ownership. Its single-writer/single-reader sequence protocol, four bounded read attempts, atomic memory orders, latest-wins coalescing, consumed-sequence gate, and explicit-zero delivery are unchanged. The public production method remains only the AudioHost publication boundary; tests reach private consumption/application through the existing friend seam.
- The audio-boundary order remains remote desired-state application first, timing-clock advancement next, local offset application next, and mapped-source anchor capture afterward. Therefore a disconnected or `NoSync` desired state cannot suppress the independent local offset, and anchors still capture only after every local take has received the offset. The helper adds no lock, allocation, wait, exception, logging, I/O, formatting, or unbounded retry.
- `NinjamDesiredTransportStateMailbox` and `NinjamTimingObservationMailbox` retain the same fields, bounded sequence checks, atomic memory orders, publication gates, coherent-value reconstruction, latest-value behavior, and `noexcept` declarations. Moving their bodies out of headers changes placement only.
- Every moved function in `NinjamTiming.h` retains its name, parameters, defaults at the declaration, return type, `noexcept` contract, arithmetic, validation, clamping, anchor-presence handling, and projection behavior. Only implementation-only standard-library includes moved with the bodies.
- Static review found no new anonymous namespace, public API class, writer multiplicity, policy/authority change, resource/generated-asset change, or B013-and-later collateral. The sole `master...HEAD` whitespace warning remains the pre-existing blank line at the end of `RemotePhaseDiscipline_Tests.cpp`, assigned to B017 and outside this batch.

The first F-012 incremental link's `LNK2005`/`LNK1169` result is not a source defect. Compilation and static-library creation succeeded, while an old dependent object still contained the formerly inline `SignedCircularDifference` definition. Touching only known dependent source mtimes produced no content diff and forced their recompilation; subsequent wrapped incremental builds, focused tests, and full-suite execution all succeeded. Canonical evidence accurately records this as stale-object recovery rather than hiding it or claiming the failed link passed.

## Independent verification

Immediately before every build and native-test invocation I reread `.vscode/tasks.json` and `doc/build.md`. The build used task-authoritative `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` only through `.github\skills\builder\invoke-msbuild.ps1`, targeting `test\JammaLib_Tests\JammaLib_Tests.vcxproj` with `/m /t:Build /p:Configuration=Debug /p:Platform=x64` and an absolute `SolutionDir` ending in exactly one backslash.

- 2026-09-02T15:03:46.0962395-06:00 through 15:03:46.9998432, final source `9473d34` with clean canonical reconciliation subsequently committed as `a2d2689`: the wrapped incremental Debug x64 native-test build completed successfully with no diagnostic output.
- 2026-09-02T15:04:02.3738767-06:00 through 15:04:02.5474809: prerequisite `TransportPhaseOffset.AbsoluteLocalOffsetIsIndependentOfNinjamGeneration` passed 1/1; failed 0; skipped 0; exit 0.
- 2026-09-02T15:04:10.8788517-06:00 through 15:04:11.1382120: exact filter `AudioHostLocalTransportOffsetPublication.*:NinjamDesiredTransportStateMailbox.*:NinjamTimingObservationMailbox.*:NinjamTiming.*:NinjamTimingCoordinator.*:TransportPhaseOffset.AbsoluteLocalOffsetIsIndependentOfNinjamGeneration` passed 50/50 across six suites; failed 0; skipped 0; exit 0.
- 2026-09-02T15:04:20.1100998-06:00 through 15:04:20.2799726: exact disconnected/`NoSync` test `AudioHostLocalTransportOffsetPublication.AppliesWhileDisconnectedAndNoSyncIsActive` passed 1/1; failed 0; skipped 0; exit 0.
- 2026-09-02T15:04:27.8372153-06:00 through 15:05:28.8687695: the full native suite ran 834 tests from 116 suites; 833 passed, `MidiDevice.OpensPreferredDeviceWhenAvailable` was the sole expected hardware-dependent skip, none failed, and the process exited 0. The inherited B009 saturation benchmark reported `timing_accepted=true`.

The worktree was clean before this review file was added. Static include, retired-symbol, project-registration, scope, memory-order, and diff-hygiene audits were performed against the exact reviewed hashes.

## Residual limitations

No interactive live NINJAM disconnect/`NoSync` session, race sanitizer, profiler, Release build, or final solution-wide audit is claimed here. The exact production-boundary test covers disconnected `NoSync` offset independence; live scenarios and broader tool/configuration coverage remain Stage 21 work and do not block this bounded ownership-only batch.
