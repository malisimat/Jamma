# B010 independent batch review

## Outcome

**APPROVE**

B010 is a surgical structural extraction with no behavior or ownership defect found. One private `noexcept` helper now contains the formerly duplicated immediate audio/MIDI cursor movement and has exactly the two approved direct callers. Both callers retain their original generation, zero-delta, consumption-count, target-delta, and applied-target gates. The queued `EndMultiPlay` path is byte-for-byte outside the implementation diff. The prerequisites, focused direct/queued coverage, and full native suite are independently green at clean canonical `96edef5c87e922762566c1c12d2079d8ee484ca1`. No B010 blocker remains; B011 may begin after this approval is reconciled into the canonical status.

## Reviewed history and range

- Pre-batch base: `308645398b2bbc99f8b22cb1c333d92b43bf3bb0` (`docs(B009 F-005 F-006 F-018 F-038): record superseding approval`).
- Passing-first characterization: `64ff124ba4845355aa80e5e0a99480e3d55a113b`.
- Private-helper implementation: `9b81a23891d9cc0f200f6436704d64e66d2797dc`.
- Canonical pending reconciliation and reviewed HEAD: `96edef5c87e922762566c1c12d2079d8ee484ca1`.
- Reviewed production/test range: `git diff 3086453..9b81a23`; full range through canonical evidence: `git diff 3086453..96edef5`. Both pass `git diff --check`.

The production/test range contains only the three approved B010 files: `JammaLib/src/engine/LoopTake.h/.cpp` and the existing `test/JammaLib_Tests/src/engine/LoopTakeTiming_Tests.cpp`. The implementation is 9 insertions and 29 deletions in production. No Station, Loop, AudioHost, NINJAM, persistence, public API, project/filter, B011 signed-offset compatibility, or later-batch file changed.

## Production and behavior assessment

No blocker was found.

- `LoopTake::_ShiftDirectPlaybackCursors(long long) noexcept` is private and has exactly two call sites: `ApplyAcceptedTimingCorrection` and `_TryApplyLocalTransportOffset`. There is no public or test-only exposure.
- `ApplyAcceptedTimingCorrection` still rejects generation zero and stale/equal generations before movement, advances `_audioTimingGeneration` before its existing zero-delta return, and increments `_consumedTimingCorrectionCount` only when the helper reports that an audio or MIDI cursor moved.
- `_TryApplyLocalTransportOffset` still calculates the delta from desired versus applied state, returns on zero delta, and advances `_appliedLocalTransportOffsetSamps` only when at least one cursor moved. This preserves the late-entity behavior for an initially empty take.
- The helper preserves each audio Loop's own `ShiftPlayIndex` modulo and computes the MIDI cursor modulo its independent `_midiVisualLoopLength`. It continues to pass the same signed translation to `_MoveMidiVisualCursor`, retaining the inverse automation-anchor correction sign rather than merging event and automation coordinates.
- Positive, negative, and zero shifts; unequal audio/MIDI lengths; direct MIDI-only, audio-only, and empty takes; local-offset audio-only, MIDI-only, and empty/late-audio behavior; automation sign; generation/accounting; queued correction; invalidation; and stale reconnect behavior are exercised by the prerequisite and focused suites.
- The implementation diff has no hunk in `EndMultiPlay`, `QueueTimingCorrection`, or `InvalidateTimingCorrections`. The queued advancement/correction ordering, generation validation, pending requeue, playable-loop gate, consumption count, MIDI advancement, and final local-offset attempt remain unchanged.
- The extracted helper contains only the pre-existing immutable audio-state snapshot read, bounded published-loop traversal, weak-reference lock, per-loop shift, atomic MIDI state access, integer modulo, and MIDI cursor move. It adds no allocation, mutex/lock primitive, wait, retry, exception flow, logging, formatting, system call, file/network I/O, or anonymous namespace. The helper itself and the existing direct callers are `noexcept` at the boundary.

## Independent verification

Immediately before every build and native-test invocation I reread `.vscode/tasks.json` and `doc/build.md`. The build used task-authoritative `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` only through `.github\skills\builder\invoke-msbuild.ps1`, targeting the absolute native-test project with `/m /t:Build /p:Configuration=Debug /p:Platform=x64` and an absolute `SolutionDir` ending in one backslash.

- 2026-09-02T12:51:18.6120895-06:00 through 12:51:20.4708177, clean canonical `96edef5`: wrapped incremental Debug x64 `JammaLib_Tests` build including references completed successfully with no diagnostic output.
- 2026-09-02T12:51:31.0925320-06:00 through 12:51:31.4839398: exact prerequisites `ExternalPhaseCorrection.SharedDeltaPreservesDifferentTakeLengths` and `TransportPhaseOffset.DirectTimingCommandMovesAudioAndMidiOnce`; passed 2/2, failed 0, skipped 0; exit 0.
- 2026-09-02T12:51:40.9310128-06:00 through 12:51:41.7177144: exact focused filter `TransportPhaseOffset.*:ExternalPhaseCorrection.*`; passed 27/27 across two suites, failed 0, skipped 0; exit 0.
- 2026-09-02T12:51:54.3994170-06:00 through 12:52:31.1669670: full native suite; 831 tests ran, 830 passed, `MidiDevice.OpensPreferredDeviceWhenAvailable` was the sole expected hardware-dependent skip, and none failed; exit 0. The inherited B009 benchmark embedded in this run also reported `timing_accepted=true`.

Static searches confirmed exactly one helper declaration, one definition, and the two intended calls; no helper occurrence exists in reset/reconnect/queued code. The reviewed production/test and full canonical ranges pass diff-check, and the worktree was clean before this review file was added.

## Residual limitations

No separate interactive NINJAM reconnect/`NoSync` session is claimed. The focused invalidation and stale-generation reconnect regressions, together with the exact two-call-site audit, demonstrate that those paths cannot invoke the helper directly. Release builds, live scenarios, sanitizers, profiler evidence, and final repository audits remain Stage 21 work and do not block this bounded structural batch.
