# B015 independent batch review

## Outcome

**APPROVE**

B015 completes F-010, F-011, F-013, F-014, F-015, F-016, and F-017 within the approved vocabulary-only boundary. All six rollback units are mechanical and preserve types, default values, aggregate field order, initializer values, formulas, synchronization, callback work, and ownership. Remote-master, local-master, local-source, device-audio, MIDI event-cursor, automation-origin, and export delay-line coordinates remain distinct. Independent build, focused, broad, and full-suite verification is green at canonical evidence tip `2b5fdb6`. No B015 blocker remains; B016 may begin after this approval is reconciled into canonical status.

No production, test, or canonical planning file was changed by this review.

## Reviewed history and ranges

- Approved pre-B015 baseline: `1ed040d` (`docs(B014 F-019 F-034 F-047): close independent review`).
- F-010 acronym casing: `e4fae40fef5ba951976a8ef9d5d8137b23b07d86`.
- F-013 remote-grid vocabulary: `7df0ee9954e4431370f03f0545b14eb4225b48b0`.
- F-014/F-015 clock-domain and correction vocabulary: `a4ef843d49139d20fddbc1e6c37577044b62e51e`.
- F-016 MIDI automation origin: `376f0b0c067da72c708a7eee2fe68f9c66258ce1`.
- F-011 metronome aggregate casing: `5a9facf5facbf6e1a1e0ba77f3a56484631c6a0b`.
- F-011/F-017 export aggregate and UI vocabulary: `03f3d1e990929b0db8ceb2730d4aceb4c674cf17`.
- Canonical pending evidence and reviewed HEAD: `2b5fdb6` (`docs(B015 F-010 F-011 F-013-F-017): reconcile vocabulary evidence`).
- Production/test range: `1ed040d..03f3d1e`; complete evidence range: `1ed040d..2b5fdb6`.

`git diff --check` passed independently for each of the six implementation commits, the complete production/test range, and the complete evidence range. The worktree was clean before this review file was created.

## Commit-by-commit audit

### `e4fae40` — F-010

- Changes only `LocalAudioGeometry::BPI` to `Bpi` and `RemoteTransportGeometry::BPI`/`BPM` to `Bpi`/`Bpm`, plus their direct consumers/tests.
- Member types, declaration order, zero defaults, validation input, atomic publication, and compared values are unchanged.
- Uppercase BPM/BPI prose remains sensible; no repository-wide acronym recasing occurred.

### `7df0ee9` — F-013

- Changes the remote-derived value from `GrainSamps` to `RemoteGridStepSamps` through tempo proposal, desired state, mailbox storage, AudioHost application, diagnostics, tests, and the remote-tempo prompt.
- `ObservationInvalidGrain` becomes `ObservationInvalidRemoteGridStep`; its external diagnostic text remains `observation-invalid-grid-step`.
- The derivation, equality comparisons, timer quantisation assignment, atomics, and default values are unchanged.
- Static search confirms remaining `GrainSamps` uses belong to local audio construction, local/MIDI quantisation, configuration, constants, or graphics. No remote-BPI consumer retains the retired grain name.

### `a4ef843` — F-014/F-015

- Desired timing now uses `RemoteMasterIntervalLengthSamps`, `RemoteMasterPhaseSamps`, `HasRemotePhaseDeviceSample`, and `RemotePhaseDeviceSample`; the complete desired-state value and its latest-wins mailbox remain intact.
- Observation publication now distinguishes `DeviceAudioSampleAtObservation` from `LocalMasterAbsoluteSampleAtObservation`. The connection snapshot, observation mailbox, coordinator projection, tracker observation, pending tempo proposal, diagnostics, and direct tests all use the matching domains.
- Boundary replacement fields are `RemoteMasterPhaseSamps` and `RemoteMasterPhaseCorrectionSamps`; tracker correction state uses the same remote-master domain. The AudioHost receipt is explicitly `LocalSourceCorrectionSamps`.
- The former AudioHost `stationDelta` is split by name into `remoteMasterCorrectionSamps` and `localSourceCorrectionSamps`. On same-geometry discipline the same computed value is assigned to both; on replacement/remap paths only the mapped local-source value reaches Stations, source-coordinate rebasing, and the applied receipt. Formula operands, signed circular-difference calls, Timer command values, generation gates, and map ordering are unchanged.
- Types, defaults, atomics, seqlock ordering, aggregate ordering, and initializer values are preserved. No device counter, Timer absolute coordinate, remote wrapped phase, or mapped source correction was collapsed into another domain.

### `376f0b0` — F-016

- Renames `MidiLoop::LoopPhaseAnchor`, `_loopPhaseAnchor`, and dispatch copies to `AutomationGlobalSampleOrigin` vocabulary, including focused test-suite names.
- The stored `startGlobalSample`, modular subtraction, live `MidiAnchorCorrection`, MIDI event cursor, loop length, and dispatch calculations are unchanged.
- Entity/map anchors and `_midiVisualPlayIndex` remain separate. This is the specifically approved automation-origin clarification, not a general Anchor/Origin rewrite.

### `5a9facf` — F-011 metronome

- Applies PascalCase to the approved public metronome input, state, onset, and result members and all direct consumers/tests.
- Field types, declaration order, zero/false defaults, fixed `std::array` capacity, sorting predicate, onset formulas, generation-reset rules, and AudioHost/NinjamMetronome control flow are unchanged.
- The only non-identifier difference is restoration of final newlines in two touched files; there is no behavioral collateral.

### `03f3d1e` — F-011/F-017 export and UI

- Applies the approved semantic names: `DelayWriteCursorSamps`, `NumFrames`, `RemoteIntervalPhaseSamps`, `RemoteIntervalLengthSamps`, `InLatencySamps`, `OutLatencySamps`, `Primed`, `LastRemoteIntervalLengthSamps`, `PredictedRemoteIntervalPhaseSamps`, `DacDelaySamps`, `AdcDelaySamps`, `Valid`, `GenerationReset`, and `AnomalyDetected`.
- Input/state/result types, member order, defaults, caller assignments, state transitions, and delay-line reads are unchanged.
- The export equations remain exactly `write cursor + output latency - remote phase (mod remote length)` and `write cursor - input latency - remote phase (mod remote length)`, followed by the unchanged `+ NumFrames`; anomaly tolerance and next-phase prediction are algebraically identical. Line wrapping and comments add no evaluation-order or numeric-domain change.
- `NinjamConnection::ExportLatencyCompensationEnabled` remains `false`, so the dormant compensation path is not enabled by this batch.
- The approved prompt labels are now `Remote grid step:` and `Remote master interval:`. The only other string change is the non-UI diagnostic field label from `observationSample=` to `remotePhaseDeviceSample=`. No unrelated UI text changed.

## Scope, ownership, and real-time assessment

No blocker was found.

- The production/test range contains only Jamma-owned timing, quantisation, command/observation, metronome, export, MIDI automation, direct-consumer, test, and approved prompt surfaces. No JSON, schema, persistence, NJClient/upstream, HUD, VST, resource, or project-registration file changed.
- Static retired-name searches are clean for remote `.BPI`/`.BPM`, remote `GrainSamps`, `ObservationInvalidGrain`, `RemotePhaseSamps`, `HasObservationSample`, `ObservationSample`, `LocalBlockStartSample`, `HasAudioBlockStartSample`, `AudioBlockStartSample`, `LoopPhaseAnchor`, `_loopPhaseAnchor`, `MidiAutomationPhaseAnchor`, `_joinDeltaSamps`, and the former lower-camel metronome/export aggregate members in their owned surfaces.
- `SyncPhaseMap`, `Ruler`, `Geometry`, `Anchor`, `Origin`, and `Mailbox` remain present where they express protected concepts. The desired-state abstraction is retained. The one Anchor-to-Origin change is exactly the separately approved F-016 mapping.
- No persisted JSON key changed and no compatibility alias or schema migration was introduced.
- Inspection of changed callback/hot-path bodies found identifier/comment/line-wrap changes only, apart from the name-only AudioHost local variable split described above. No allocation, lock, wait, I/O, formatting, exception path, unbounded traversal, ownership transfer, or anonymous namespace was added.

The three timing documents intentionally still contain pre-B015 vocabulary in places. That statement reconciliation is explicitly gated to B016 and is not a B015 production/test defect.

## Independent verification

Immediately before every build or native-test invocation I reread `.vscode/tasks.json` and `doc/build.md`. The build used task-authoritative `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` only through `.github\skills\builder\invoke-msbuild.ps1`, targeting `test\JammaLib_Tests\JammaLib_Tests.vcxproj` with `/m /t:Build /p:Configuration=Debug /p:Platform=x64` and `/p:SolutionDir=C:\Users\matto\OneDrive\Source\Jamma\`, with exactly one trailing backslash. Windows PowerShell 5.1 required the previously established process-local `ProcessArgumentListShim`; the repository wrapper was not changed.

- Wrapped incremental Debug x64 native-test build: start `2026-09-05T20:13:38.2935638-06:00`, end `20:13:44.6830371-06:00`; wrapper success; process exit 0.
- Exact prerequisites: `--gtest_filter=NinjamTimingCoordinator.AcceptedRemoteGridRetainsAuthoritativeBpi:TransportPhaseOffset.DirectTimingCommandKeepsMidiAutomationWithNoteCursor`; start `20:14:27.8176625-06:00`, end `20:14:28.0182133-06:00`; 2/2 passed, 0 failed, 0 skipped, exit 0.
- Expanded broad filter: `NinjamTiming.*:NinjamTimingInput.*:NinjamTimingCoordinator.*:NinjamTimingDiagnostics.*:NinjamTimingTracker.*:NinjamTimingObservationMailbox.*:NinjamDesiredTransportStateMailbox.*:NinjamLoopAlignment.*:TimerApplyCommand.*:NinjamTimingProductionBoundary.*:AudioHostLocalTransportOffsetPublication.*:NinjamSessionTiming.*:NinjamNetworkServiceTiming.*:TransportPhaseOffset.*:ExternalPhaseCorrection.*:MusicalTransport.*:MidiAutomationGlobalSampleOrigin.*:MidiAutomationLaneResolution.*:MidiLoop.*:MidiQuantisation.*:Quantisation.*:NinjamMetronomeTiming.*:NinjamTempoCommand.*:NinjamMetronome.*:ExportLaneTiming.*`; start `20:15:17.8026739-06:00`, end `20:15:44.4598605-06:00`; 227/227 passed across 25 suites, 0 failed, 0 skipped, exit 0. The inherited B009 callback benchmark reported `timing_accepted=true`.
- Full native suite: start `20:16:08.3244010-06:00`, end `20:16:40.8180683-06:00`; 844 tests from 117 suites ran, 843 passed, `MidiDevice.OpensPreferredDeviceWhenAvailable` was the sole expected hardware-dependent skip, 0 failed, exit 0. The inherited B009 callback benchmark again reported `timing_accepted=true`.

## Residual limitations

No live NINJAM prompt, replacement/discipline coordinate trace, late-observation session, automation playback trace, enabled export-compensation run, or other manual audio scenario was executed. No Release build, profiler, sanitizer, race tool, page heap, or Application Verifier run was part of this batch review. Those remain explicit Stage 21 obligations or accepted residuals; they are not represented as passed here.
