# B006+B007 independent batch review

## Outcome

**REJECT**

The atomic implementation is behaviorally sound in the reviewed production boundary, P1/P2/P3 and the protected regressions pass, and the full native suite is green. Two in-scope evidence/hygiene defects prevent approval: the batch did not remove the specifically assigned trailing EOF blank from `NinjamTimingIntegration_Tests.cpp`, and the canonical 107-test focused-run record names a filter that currently selects only 99 tests. Neither defect requires a production change or boundary expansion, but both must be corrected before B008.

## Reviewed history and range

- Pre-batch amendment/base: `ca4ec9b` (`docs(phase4): merge B006 and B007 execution boundary`).
- Characterization-only prerequisite: `46816af3c01242039993ecc5690821fffb5e76f5`.
- Initial atomic implementation: `f1e0df15987d039a9a7162dd59c3535f6bb102eb`.
- Audit-correction characterization: `34918aa7eca15e164ecf0e5ae841e2b0468b3302`.
- Corrected implementation: `da0db2459976f2589f2085f0179acd1262944e69`.
- Canonical reconciliation and reviewed HEAD: `6a1295e734cf368414b591cf68cc097e53fa0a47`.
- Exact review range: `git diff ca4ec9b..6a1295e`, plus the clean files at reviewed HEAD. `git diff --check ca4ec9b..6a1295e` is clean; the complete-merge audit is not, as described below.

The source/test range is confined to the combined owned boundary: the desired-state value/mailbox, Coordinator/NetworkService, narrow Scene forwarding removal, AudioHost desired/applied processing, Station/LoopTake epoch reset, and the named production-boundary/command/timing tests. The remaining changes reconcile the canonical review artifacts. No B008 empty-edge movement, B009 common-map consolidation, persistence, UI redesign, upstream NJClient, HUD/resource, generic dispatcher/class, naming sweep, or diagnostics-formatting implementation entered the batch.

## Production and test assessment

No production blocker was found.

- **One serialized producer:** every complete-state construction occurs in `NinjamTimingCoordinator::_PublishRemoteDesired` or `_PublishNoSync` (`JammaLib/src/ninjam/NinjamTimingCoordinator.cpp:448`-`:493`). Every public production entry into that coordinator is serialized by `NinjamNetworkService::_timingMutex` (`JammaLib/src/ninjam/NinjamNetworkService.cpp:31`-`:61`, `:163`-`:200`). `_desiredVersion` is not reset on reconnect, while the B005 session epoch is accepted only when it increases (`JammaLib/src/ninjam/NinjamTimingCoordinator.h:200`-`:212`; `.cpp:69`-`:90`). Prompt, request, observation, and deadline precedence remain inside this serialized owner.
- **Scene is forwarding/presentation only:** all Scene timing call sites receive `NinjamTimingUpdate`; `_ApplyNinjamTimingUpdate` logs/presents, forwards the producer-owned `RemoteGrid`, and publishes the already-complete desired value (`JammaLib/src/engine/Scene.cpp:249`-`:423`, `:1327`-`:1355`). It no longer constructs transport authority, geometry, phase delta, generation, invalidation, or policy.
- **One latest desired/applied consumer:** `AudioHost::PublishDesiredTiming` serializes non-audio publishers and rejects older epoch/version values before writing one fixed latest-value mailbox (`JammaLib/src/audio/AudioHost.cpp:113`-`:125`; `JammaLib/src/ninjam/NinjamAudioTimingCommand.h:26`-`:124`). The audio boundary rejects old epochs, non-newer versions, and equal/stale within-epoch generations while allowing a fresh epoch to restart at generation 1 (`JammaLib/src/audio/AudioHost.cpp:151`-`:166`). Its application seam is private and exposed only to the named friend accessor (`JammaLib/src/audio/AudioHost.h:90`-`:99`; `test/JammaLib_Tests/src/ninjam/NinjamTimingIntegration_Tests.cpp:26`-`:37`), not as public test-only API.
- **Replace/discipline/reset semantics:** epoch or `NoSync` clears Timer authority, the AudioHost map, Station/LoopTake NINJAM generation gates, and entity anchors without moving cursors (`JammaLib/src/audio/AudioHost.cpp:168`-`:188`; `JammaLib/src/engine/Station.cpp:764`-`:771`; `JammaLib/src/engine/LoopTake.cpp:629`-`:633`). Geometry changes replace Timer timing; unchanged geometry applies phase discipline; restoration precedes correction and rebase (`JammaLib/src/audio/AudioHost.cpp:190`-`:284`). `LoopTake::ResetTimingEpoch` deliberately leaves the separate queued local correction fields untouched.
- **Protected per-entity state:** corrected P3 constructs real audio Loops and MidiLoops at `M`, `2M`, and `3M`; checks MIDI cursor and automation correction independently; preserves all cursors through `NoSync`; advances all entities while disconnected; accepts epoch-2 generation 1; and rejects old-epoch, equal-generation, and stale-generation values even when their publication version is newer (`test/JammaLib_Tests/src/ninjam/NinjamTimingIntegration_Tests.cpp:315`-`:396`). P2 concurrently calls the real NetworkService/Coordinator producer and proves each desired/grid pair coherent and versions consecutive, then proves delayed older publication cannot supersede the newer value (`:229`-`:313`). P1 exercises both formerly lossy histories through the real AudioHost/Timer boundary (`:183`-`:227`). The prior pseudo-model is absent.
- **Audit corrections:** the initial implementation incorrectly cleared unrelated queued local correction state and exposed the boundary publicly. The `34918aa` regression and `da0db24` correction reduce reset to NINJAM generation/anchor/map state, retain the local correction queue, make the seam private, route P2 through the real producer, and add real MIDI/free-run coverage. `ExternalPhaseCorrection.QueuedLocalCorrectionSurvivesNinjamEpochReset` independently passes.
- **Callback safety:** the changed audio-boundary path uses fixed-value atomics, bounded mailbox reads, the already published immutable Station/LoopTake snapshots, and bounded per-snapshot traversal. Static diff/search found no added callback allocation, lock, wait, formatting, logging/I/O, final destruction, or anonymous namespace. The publication mutex is used only by the non-audio `PublishDesiredTiming` entry and is not acquired by the callback reader.

## Blocking review findings

### B006+B007-R1 — assigned EOF cleanup remains incomplete

The merged batch verification contract explicitly requires removing the Stage 21 trailing EOF blank from its owned `test/JammaLib_Tests/src/ninjam/NinjamTimingIntegration_Tests.cpp`. The current file still ends with a blank line after line 396. Consequently `git diff --check master...HEAD` reports:

```text
test/JammaLib_Tests/src/ninjam/NinjamTimingIntegration_Tests.cpp:397: new blank line at EOF.
```

The separate `RemotePhaseDiscipline_Tests.cpp:156` report remains correctly assigned to B017. Remove only the integration-test EOF blank in a narrow B006+B007 correction commit, then rerun the affected gates and both range checks.

### B006+B007-R2 — the recorded 107-test filter is not reproducible as written

`cleanup-backlog.md` and `verification-matrix.md` record 107/107 for this exact filter:

```text
NinjamTimingProductionBoundary.*:NinjamTimingCoordinator.*:NinjamSessionTiming.*:NinjamNetworkServiceTiming.*:NinjamDesiredTransportStateMailbox.*:NinjamTiming.*:NinjamTimingObservationMailbox.*:NinjamLoopAlignment.*:TimerApplyCommand.*:TransportPhaseOffset.*:MidiClockAnchor.*:MidiBlockTiming.*:MidiAutomationPhaseAnchor.*:Vst2PluginHostCallback.*:VstAudioBuffers.*
```

At clean `6a1295e`, that exact filter passes but selects 99 tests from 15 suites, not 107. Broader independent probes also pass, but do not make the stated command/result pair true. Reconcile the canonical record to the command actually run or to the reproducible 99/99 result; do not change production code for this documentation defect.

## Independent verification

Immediately before every build or native-test invocation I reread `.vscode/tasks.json` and `doc/build.md`. The build used task-authoritative `C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe` only through `.github\skills\builder\invoke-msbuild.ps1`, targeting the absolute native-test project with `/m /t:Build /p:Configuration=Debug /p:Platform=x64` and an absolute `SolutionDir` ending in one backslash.

- 2026-09-02T09:44:28.5945341-06:00, clean `6a1295e`: wrapped incremental Debug x64 `JammaLib_Tests` build including references, exit 0.
- 2026-09-02T09:44:40.9208483-06:00: P1/P2/P3 plus `ExternalPhaseCorrection.QueuedLocalCorrectionSurvivesNinjamEpochReset`; passed 4, failed 0, skipped 0 across 2 suites; exit 0.
- 2026-09-02T09:44:52.8425738-06:00: the exact canonical focused filter quoted above; passed 99, failed 0, skipped 0 across 15 suites; exit 0.
- 2026-09-02T09:45:18.1906659-06:00: B004 P5/P6 and the three B005 lifecycle/deadline contracts; passed 5, failed 0, skipped 0 across 5 suites; exit 0.
- 2026-09-02T09:59:13.3021989-06:00: full native executable with brief output; 824 tests ran, 823 passed, `MidiDevice.OpensPreferredDeviceWhenAvailable` was the one expected hardware-dependent skip, 0 failed; exit 0.
- Additional green breadth probes selected 102 tests after adding `NinjamTimingTracker.*`, 127 after also adding `Timer.*`, and 109 after adding tracker/MIDI timestamp/VST3 mapping suites. These probes confirm surrounding health but are not substituted for the canonical command/result correction.

Static audits found no former `NinjamClockSettings`, `NinjamPhaseCorrection`, `InvalidatePendingCorrections`, or event/delta command type in JammaLib or the native tests. `git diff --check ca4ec9b..6a1295e` is clean, the worktree was clean before this review file, and all reviewed production/test paths remain inside the merged ownership boundary.

## Residual limitations

No live NINJAM server, UI prompt trace, physical cable loss/retry, runtime disconnect/reconnect, race sanitizer, ASan, page heap, Application Verifier, or maximum-hierarchy profiler run is claimed. Release/final solution builds and remaining project/resource/local-artifact/generated-asset audits remain Stage 21 work. These are explicit deferred residuals, not the reason for this rejection.
